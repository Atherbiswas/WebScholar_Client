import React from 'react';
import logo from '../../caurosel img/error.png'

const NotFound = () => {
    return (
        <div>
            <img style={{width: '100%'}} src={logo} alt="" />
        </div>
    );
};

export default NotFound;